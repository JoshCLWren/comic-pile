name: Fixed Model Factory Dispatcher

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Factory automation paused
        run: echo 'Fixed-model factory dispatch is intentionally paused via .github/FACTORY_AUTOMATION_PAUSED.'
