name: Chromium E2E discovery

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Factory automation paused
        run: echo 'Chromium discovery is intentionally paused via .github/FACTORY_AUTOMATION_PAUSED.'
