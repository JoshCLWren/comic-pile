name: Factory heartbeat watchdog

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Factory automation paused
        run: echo 'Factory heartbeat monitoring is intentionally paused via .github/FACTORY_AUTOMATION_PAUSED.'
