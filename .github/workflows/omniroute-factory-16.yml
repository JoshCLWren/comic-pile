name: OmniRoute Factory 16 Experiment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - name: Experiment status
        run: echo 'OmniRoute Factory 16 is scaffolded but intentionally parked until the worker wrapper is reviewed and added.'
