# Handoff Context
Generated: 2026-03-27T20:15:16Z
Model: nvidia/qwen/qwen3-coder-480b-a35b-instruct

## What we were working on
Writing documentation for the opencode orchestration pipeline — the multi-role bash daemon in `scripts/opencode_pipeline.sh` that autonomously processes GitHub issues through implement → review → fix → pr → ci_check → merge. The user wants this documented so future contributors (and agents) can understand how it works.

## What has been completed
The pipeline itself is feature-complete. Recent additions this session:
- `cmd_merger`: auto-merges fully-green pipeline/* PRs one at a time, rebases remaining branches onto main
- `cmd_qa`: Playwright MCP QA agent — starts dev-all if needed, tests app, files GH issues for bugs
- `cmd_main_fixer`: surveys CI across all PRs, fixes main-branch regressions via opencode
- `_candidate_models()`: dynamic model discovery via `opencode models` instead of static lists
- Per-model exponential backoff with 6h cap and eviction; rate-limit gets flat 3h backoff
- Two-tier model pool: Tier 1 (TOOL_OK) for implement/review/fix; Tier 2 for pr/ci_check
- Fixed `model_in_backoff()` to auto-delete expired backoff files (was causing 0% success rate)
- 5 PRs merged today (#384, #395, #396, #398, #399)

## What is pending / needs doing next
**Primary task: Document the orchestration system.**

Write a `PIPELINE.md` (or update an existing doc) that covers:

1. **Overview** — what the pipeline does, why it exists, the philosophy
2. **State machine** — the full issue lifecycle with a diagram or ASCII art
3. **Roles** — one section per role (implement, review, fix, pr, ci_check, arbiter, merger, qa, main_fixer, model_manager, tool_test) with:
   - What it does
   - What state(s) it reads from / transitions to
   - How to run it
   - Key env overrides
4. **Model pool** — Tier 1 vs Tier 2, how models are discovered, backoff system, eviction
5. **Worktrees** — how per-issue git worktrees work, where they live, cleanup
6. **Running the pipeline** — quick-start guide (which workers to start, how many, in what order)
7. **Monitoring** — `status`, `timesheet`, log files, what healthy looks like vs stalled
8. **Troubleshooting** — common failure modes and fixes (all-models-in-backoff, stuck states, CI failures, node_modules committed, etc.)

## Key files
- `scripts/opencode_pipeline.sh` — the entire pipeline (~1600 lines); read this thoroughly
- `.opencode.json` — opencode project config (Playwright MCP configured)
- `Makefile` — `make dev-all`, `make lint`, `make pytest`, `make dev`, etc.
- `.opencode_logs/` — runtime state: `issue_N/state`, `.model_backoff/`, `timesheet.jsonl`, `*.log`
- `.opencode_logs/model_tool_test_results.txt` — Tier 1 pool (TOOL_OK lines)
- `.opencode_logs/model_test_results.txt` — Tier 2 pool
- `CLAUDE.md` — project-wide conventions (read this)

## Pipeline state
- Workers: 42 running (implement×16, review×4, fix×4, pr×3, ci_check×5, arbiter×1, model_manager×1, merger×1, qa×1, main_fixer×1)
- Active issues: #363 implementing, #371 reviewing, #377/#378/#380 reviewed, #379 fixing
- Merger running, QA running (first run found bugs #401, #403)
- 99 total successes (all historical), 0 today — backoff bug was just fixed, pipeline recovering

## Errors / blockers
None blocking documentation. For context:
- Pipeline had 0% success today due to expired backoff files not being cleaned (now fixed)
- QA agent found 4 bugs: die selector (#401/#402), fixture failures (#403/#404) — #402 and #404 were dupes, closed

## Notes for the next agent
- The pipeline script has a dispatch table at the very bottom — good starting point to understand all roles
- The state machine comment near the top of the script gives the flow
- Each `cmd_*` function is ~50-200 lines with a comment block explaining it — read those headers
- `run_with_fallback()` is the core function that actually calls opencode with model rotation + backoff
- Backoff formula: `15 * 2^(fail_count-1)` seconds, capped at 21600s (6h); rate-limit = flat 3h
- Worktrees live at `~/.opencode-worktrees/comic-pile/issue_N` on branch `pipeline/issue-N`
- `make lint` runs `npm run lint` (frontend) + `ruff` (Python); must pass before committing
- Do NOT touch `~/.config/opencode/` — user manages their own auth
- Write the doc as `PIPELINE.md` in the repo root unless a docs/ directory exists
- Aim for ~400-600 lines of clear markdown — comprehensive but not padded
- Include the ASCII state machine diagram
- The doc is for both humans and future AI agents — write it so an agent with no prior context can pick up and run the pipeline
