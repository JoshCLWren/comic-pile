# Handoff Context
Generated: 2026-03-27T19:40:22Z
Model: nvidia/z-ai/glm4.7

## What we were working on
Maintaining and improving the opencode pipeline (`scripts/opencode_pipeline.sh`) for the comic-pile repo — a multi-role bash daemon that processes GitHub issues through implement → review → fix → pr → ci_check → merge. We added a `merger` role (auto-merges green PRs), a `qa` role (Playwright MCP QA agent), and were investigating why pipeline success rate is 0% today despite models working fine when called directly.

## What has been completed
- Added `cmd_merger` to pipeline: polls for CLEAN/MERGEABLE pipeline/* PRs, merges one per cycle (squash), rebases remaining branches onto updated main — **5 PRs merged today** (#384, #395, #396, #398, #399)
- Fixed merger bugs: removed `--yes` flag, removed `--delete-branch` (conflicts with active worktrees), switched to `rm -rf` for node_modules cleanup before rebase
- Added `cmd_qa` to pipeline: starts `make dev-all` on QA ports (8002/5175) if not running, runs opencode with Playwright MCP, files GH issues for failures, closes server when done
- Fixed QA dedup logic: agent now fetches existing qa: issues upfront and tracks filed issues in-memory to avoid duplicates
- Closed duplicate QA issues (#402, #404 were dupes of #401, #403)
- Merger and QA daemons are running (pids in .opencode_logs/.merger_lock, .opencode_logs/.qa_lock)

## What is pending / needs doing next

### MOST URGENT: Pipeline has 0 successes today despite models working
- `nvidia/z-ai/glm4.7` works fine when called directly (`opencode run -m nvidia/z-ai/glm4.7 "say hello"` → Hello!)
- But pipeline shows 0 successes / 44,399 failures
- Root cause partially identified: 26 models in active backoff, 30 already expired but backoff files not cleaned up
- The workers may be treating expired backoffs as active due to a bug in the backoff check
- **Action needed**: investigate why expired backoffs aren't being cleared; check if `model_in_backoff()` has a bug; consider running `rm -rf .opencode_logs/.model_backoff/` to reset all backoffs and restart workers
- Also check: are worker processes actually running? `pgrep -fa "opencode_pipeline"`

### Pipeline stuck issues
- Issue #363: `implementing` state, last model `nvidia/z-ai/glm4.7`, 1m ago — worktree has glob error (`No such file or directory: .../issue_363/src` — model ran `cd frontend && npm test` but frontend/src is at `frontend/src` not `src`)
- Issue #403, #401: `missing` state — these are QA issues filed by the qa agent, not pipeline issues; should be ignored by pipeline
- 26/42 active models in backoff, reducing available pool severely

### Model pool health
- 42 Tier 1 (TOOL_OK) models in pool (down from 53 last tool_test)
- 26 in active backoff right now
- Run `bash scripts/opencode_pipeline.sh tool_test` to rediscover working models
- Consider clearing backoffs: `rm -rf .opencode_logs/.model_backoff/ && echo "cleared"`

### QA agent
- First QA run (20260327_190001) is still running — it registered a test user, ran auth/queue/history/threads spec files, found failures (die selector not visible, fixture registration errors, auth token persistence)
- QA issues filed: #401 (roll page die selector), #403 (fixture registration failures)
- Frontend (port 5175) failed to start because something on port 8000 conflicts (cie-api uses 8000; QA uses 8002 now, but Vite on 5175 also died — needs investigation)
- QA runs every 3600s

### Open PRs needing attention
Currently open pipeline PRs (from `gh pr list`):
- PRs in reviewed state (#377, #378, #380) — need fixing role to pick up
- PR for #371 — reviewing
- PR for #379 — fixing

## Key files
- `scripts/opencode_pipeline.sh` — the entire pipeline (implement/review/fix/pr/ci_check/arbiter/merger/qa/main_fixer/model_manager roles)
- `.opencode_logs/` — all logs, state files, backoff files, timesheet
- `.opencode_logs/.model_backoff/` — per-model backoff files (`fail_count\ntimestamp` or `ratelimit\ntimestamp`)
- `.opencode_logs/model_tool_test_results.txt` — Tier 1 model pool (TOOL_OK lines)
- `.opencode_logs/model_test_results.txt` — Tier 2 model pool
- `.opencode_logs/timesheet.jsonl` — per-run outcome log (ts, issue, role, model, duration_s, outcome)
- `.opencode_logs/issue_N/state` — current state of each issue
- `.opencode_logs/merger.log` — merger daemon log
- `.opencode_logs/qa.log` — qa daemon log
- `.opencode.json` — opencode config with Playwright MCP configured
- `Makefile` — `make dev-all` starts both servers (backend 8000, frontend 5173)

## Pipeline state
- Workers running (37 total started earlier): arbiter, implement×23, review×2, fix×2, pr×5, ci_check×8, model_manager
- Merger running (pid in .opencode_logs/.merger_lock)
- QA running (pid in .opencode_logs/.qa_lock)
- Stats: 44,782 sessions, 99 successes (all from days ago), 44,399 failures, 284 timeouts

## Errors / blockers
- **Zero successes today** — most likely cause: expired backoff files not being cleaned up, combined with model API errors causing rapid re-backoff
- `No such file or directory: .../issue_363/src` — model running `npm test` from wrong directory
- QA frontend not starting on port 5175 (Vite process dies) — likely another port conflict or missing node_modules
- `/handoff` skill showing "Unknown skill" — the global skill at `~/.claude/commands/handoff.md` may not be registered correctly (user said it's a project skill issue)

## Notes for the next agent
- **Backoff formula**: `backoff_s = 15 * 2^(fail_count-1)`, capped at 21600s (6h); at cap, model is evicted from pool files
- **Rate-limit backoff**: flat 3h, stored as `ratelimit\ntimestamp` in backoff file
- **State machine**: `pending → implementing → implemented → reviewing → reviewed → fixing → fixed → pr_open → pr_opened → ci_checking → done` (merger then merges `done` PRs)
- **Worktrees**: each issue has `~/.opencode-worktrees/comic-pile/issue_N` on branch `pipeline/issue-N`
- **Never touch `~/.config/opencode/`** — user manages their own auth config
- **node_modules**: some worktrees have a stray `node_modules` symlink committed (historical issue); pre-commit hook blocks it but the symlink may still exist on disk
- **Port assignments**: backend=8000 (or 8002 for QA), frontend=5173 (or 5175 for QA), cie-api=8000 (conflict!)
- The merger uses `--squash` strategy; it merges one PR per cycle then sleeps 120s
- zai-coding-plan models: ~2hr heavy-use window before 3hr rate limit; nvidia/z-ai/glm4.7 has no such limit
