---
description: Full-repo ruff and ty before every Python commit or push
alwaysApply: true
---

# Python CI lint gate

CI jobs `python-lint` and `python-typecheck` run exactly:

```bash
ruff check .
ty check --error-on-warning
```

Path-filtered ruff or ty is not a valid CI substitute.

Before every Python commit or push, run `bash scripts/check-python-ci-lint.sh` (or those two commands with no path arguments) against the whole repo. Re-run after every later Python edit. A green run from before the last edit does not count. Do not use `--no-verify`.

Before the first Python commit of a session, run `bash scripts/install-git-hooks.sh` so Cursor's remapped `core.hooksPath` can still chain to `.git/hooks/pre-commit`.
