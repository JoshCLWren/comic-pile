{
  "type": "module",
  "name": "comic-pile",
  "version": "0.1.0",
  "description": "Dice-Driven Comic Reading Tracker - FastAPI + HTMX + Tailwind CSS",
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^15.13.0",
    "htmlhint": "^1.1.5",
    "jiti": "^2.6.1",
    "oxlint": "1.82.0",
    "testdriverai": "^7.11.58-test",
    "vitest": "^4.1.11"
  },
  "scripts": {
    "build": "pnpm --filter frontend run build",
    "audit:ui": "bash scripts/run_ui_audit.sh",
    "lint:js": "echo No JavaScript files to lint",
    "lint:html": "pnpm exec htmlhint app/templates/*.html",
    "lint": "pnpm run lint:js && pnpm run lint:html",
    "lint:fix": "sh -c 'if ls static/react/assets/*.js >/dev/null 2>&1 || ls static/assets/*.js >/dev/null 2>&1; then pnpm exec eslint static/react/assets/*.js static/assets/*.js --fix; else echo \"No JavaScript files to fix\"; fi'",
    "lint:anti-slop": "oxlint -c .oxlintrc.json frontend",
    "dice:playground": "pnpm --filter frontend run dice:playground",
    "test:dice-playground": "pnpm --filter frontend run test:dice-playground",
    "test:dice-playground:update": "pnpm --filter frontend run test:dice-playground:update",
    "test:e2e:prod-smoke": "pnpm --filter frontend run test:e2e:prod-smoke",
    "precommit": "pnpm run lint",
    "prepublish-checks": "pnpm run lint && pnpm run typecheck"
  }
}
