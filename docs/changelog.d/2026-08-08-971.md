## 2026-08-08

### Performance and reliability

- [#971](https://github.com/JoshCLWren/comic-pile/pull/971) adds the bounded, user-scoped cache-generation primitive needed to invalidate cached data without scanning the Redis keyspace. It keeps cache reads coherent across concurrent invalidation and correctly preserves cached empty results, while remote caching remains disabled until the later migration and measurement work is complete.
