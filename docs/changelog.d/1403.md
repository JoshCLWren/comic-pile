## 2026-08-19

**ComicVine Issue Intelligence in Roll Comic Pillar (#1403)**

- The Comic pillar now displays a large cover image and expanded ComicVine metadata by default (series name, issue number, story title, cover/store date, creators with roles, description, story arcs, and ComicVine source link)
- Mobile shows essential identity immediately with secondary details (creators, story arcs, related issues) collapsible to keep the rating workflow reachable
- Missing cover images gracefully fall back to a placeholder; missing metadata produces clean fallbacks without blocking the rating UI
- No mockup-only metadata is invented; publisher, price, UPC, and other unsourced fields are omitted
- All data comes from the existing DB-first `useComicVineIssueIntelligence` hook — no synchronous external ComicVine calls
- Refactored `ComicVineIssueCard` to match the new first-class visual design (previously a small collapsed card)
- Focused component tests cover full, partial, missing, and long metadata states