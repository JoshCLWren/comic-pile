# Review priority

After the production run, review active Roll-impacting `needs_review` rows first, then unnoted rows, then non-uniform named families, then satisfied historical families. Priority affects review order only, never whether a row must be classified.
