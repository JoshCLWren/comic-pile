# Unnoted dependencies

Rows with no note require endpoint/context evidence. They are never inferred from series relationship or creation order. The first-pass audit leaves them `needs_review` by design.
