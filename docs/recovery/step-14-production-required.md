# Production evidence required

A local/test ledger cannot satisfy #2445. Acceptance is specifically about the live user-1 legacy dependency state. The production run is read-only, but it is mandatory.
