# Dependency groups

Group/crossover membership and `sequence_order` are evidence only. They do not become reader-intent authority. Final ledger enrichment must record shared group membership and sequence values without mutating them.
