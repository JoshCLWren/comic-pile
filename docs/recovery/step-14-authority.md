# Authority boundary

The ledger describes legacy runtime inputs. It does not promote them to canonical reader intent. Any later migration must derive canonical Reading Plan state from reviewed reader intent/source evidence, then prove runtime equivalence before retiring covered legacy rows.
