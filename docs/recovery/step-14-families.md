# Family classification

Large families may be classified together only after invariant checks prove every row in that exact dependency-ID set has the same semantics. Family summaries never replace row enumeration.
