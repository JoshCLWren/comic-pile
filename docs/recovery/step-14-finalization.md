# Finalization sequence

Fresh production run → evidence enrichment → explicit review decisions → fresh rerun → exact reconciliation → persist ledger/summary → update #2445 → update #2366 → only then re-evaluate downstream migration gates.
