# Proof standard

A classifier is accepted only when its invariants prove the semantics of every dependency ID it covers. Pattern names are evidence, not proof. Every family classifier must preserve exact dependency-ID accounting so totals can be independently reconciled to the snapshot.
