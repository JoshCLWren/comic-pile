# Why Step 14 exists

The taxonomy was decided earlier, but production state was never exhaustively classified. More than one hundred thousand legacy hard-blocking rows mix source ordering, explicit prerequisites, incident repairs, historical state, and unknown semantics. Migration without this accounting would reintroduce the same architectural guessing the recovery is meant to stop.
