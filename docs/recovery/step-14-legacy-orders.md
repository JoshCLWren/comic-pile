# Legacy Reading Order overlap

Doctor Strange, Starman, and JSA remain compatibility/import state until their overlapping dependency semantics are known. The ledger records legacy Reading Order overlap so those migrations can later proceed one at a time without guessing.
