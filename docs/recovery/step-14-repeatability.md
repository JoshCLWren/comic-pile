# Repeatability

The snapshot token is derived from the ordered ledger. Any production change or classifier/evidence change requires a fresh complete run. Never splice classifications from different production snapshots into one accepted result.
