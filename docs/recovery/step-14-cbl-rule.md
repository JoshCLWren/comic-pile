# CBL rule

Exact `cbl-order:source:*` is currently the only automated `reading_plan_order` classifier. Even that classification is only acceptable for final sign-off once the production ledger also proves persisted source identity/endpoints/execution role for the covered rows. No broader note prefix inherits this classification automatically.
