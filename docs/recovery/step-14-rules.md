# Canonical rule linkage

A `continuity_rules.legacy_dependency_id` link proves runtime mirroring/provenance, not whether the legacy dependency should survive. Linked rows therefore remain review-required unless independent invariants establish their semantics.
