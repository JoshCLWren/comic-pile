# Issue #1389 Plan: Optimize FastAPIBackend cold starts on Vercel Hobby tier

## Scope
- Add lightweight `GET /api/ping` endpoint (`app/api/ping.py`) with zero DB/ORM dependency
- Register ping router in `app/main.py`
- Create `frontend/src/hooks/usePingHeartbeat.ts` with visibility-aware 4-min polling
- Integrate hook in `frontend/src/App.tsx`
- Update `vercel.json` with `excludeFiles` and `crons`

## Key files
- `app/api/ping.py`
- `app/main.py`
- `frontend/src/hooks/usePingHeartbeat.ts`
- `frontend/src/App.tsx`
- `vercel.json`

## Risks
- Must not import DB/ORM in ping router at import time
- Hook must respect `document.visibilityState`
- Must keep existing tests passing
