# Issue #2777 Plan — Password reset 1/3

1. Add `PasswordResetToken` model + `password_reset_tokens` table.
2. Add `password_changed_at` to `users` (for JWT revocation after reset).
3. Migration `alembic/versions/2777_password_reset.py`.
4. Repository `app/repositories/password_reset_token_repository.py`.
5. Service `app/services/password_reset_service.py` with forgot + reset + handoff + revocation.
6. Schemas in `app/schemas/auth.py`.
7. Endpoints in `app/api/auth.py`: `/forgot-password`, `/reset-password`.
8. Update `app/auth.py` to include `password_changed_at` in JWT and verify.
9. Focused tests `tests/test_password_reset.py`.
10. Python lint + focused pytest validation.
